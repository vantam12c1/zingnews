import React from 'react';
import PropTypes from 'prop-types';

NewBook.propTypes = {
    
};

function NewBook(props) {
    return (
        <div>
            
        </div>
    );
}

export default NewBook;